This is a minimal (read cheap to implement) USB-PD core.

The aim is to make a CC pin protocol interface using a level converter chip with tristate output, a couple IO inputs and, optionally, a ramp ADC to read voltages.

